# Vanguard skill performer candidate

State: v2 CANDIDATE_RUNTIME_TRIAL; existing portrait and R2 assets remain preserved. This is not final-art approval or a smooth-animation acceptance.

Generation result: builtin image model produced `source-chroma.png`, then one framing correction produced `source-chroma-v2.png` (generation `exec-315aa8b7-eac4-4ba2-a048-b4775470ef97`). The same keying recipe derived `performer-atlas-v2.png`,1344×1344 RGBA,1,222,595 fully transparent pixels and19,502 partial-alpha pixels. All9 corrected source cells have zero side-contact pixels; v1 has3 edge-contact frames and is not bound. Visual review retained the face/costume and found no baked checkerboard. V2 source/derived hashes are recorded in `manifest-v2.json`.

Correction prompt: preserve all9 poses/style, reduce cramped framing, reconstruct clipped scarf/weapon/effect tips, add pure-green cell gutters and bottom margins. Mechanical processing only removes chroma/spill and packs model pixels; it does not redraw or invent in-between frames.

Aseprite restricted candidate copy/readback succeeded: `performer-master-v2.aseprite`,1344×1344 RGB, **one editor frame containing9 atlas poses**, not an authored9-frame timeline. `r3_performer.gd` validates the atlas hash/size and resolves category/phase regions; unknown states return null and the screen retains the portrait fallback. Native fixture confirmed actual DEF_WARD receipt, impact frame448,448,448,448 and no asset errors. Remaining: organic play, temporal continuity, scale/readability across settings, complete smooth motion and final user art review. Native fixture is not real player completion.

Exact intended consumer: `res://scenes/replanned_r3/main.tscn::Combat/CutIn/Actor` via `src/replanned_r3/r3_screen.gd`. Target source `source-chroma.png`, derived `performer-atlas.png` in this directory. Nine equal square cells: rows ATK/DEF/SUP, columns anticipation/impact/recovery. Bottom-center anchor, full arms/weapon inside each cell with padding. Display about225×240 logical pixels, filtered texture, true RGBA; no opaque key background or baked checkerboard accepted. Foreground creative pixels must come from the image model, not generated geometric substitutes. Preserve source/hash and inspect per-cell matte/pivot/identity before binding.

Reference: repository `blueprint/portrait-states.png` preserves black-haired/brown-eyed young Vanguard with dark steel/gold armor and teal scarf. User skill GIF supplies entrance/casting/exit staging only; none of its characters or effects is copied. Generated key poses are not proof of smooth in-between animation.

Reuse preflight: ADOPT existing session receipt/presentation clock and existing portrait identity; ADAPT new category-specific body poses to the bounded combat-stage consumer. Existing portrait-only cut-in cannot communicate a cast gesture. Alternatives: portrait slide (cheap but lacks requested action), nine keyed authored poses plus timed transition (selected bounded candidate), full skeletal rig (deferred until source layers/rig exist). No new framework or paid API. [Godot AnimatedSprite2D official documentation](https://docs.godotengine.org/en/stable/classes/class_animatedsprite2d.html), read2026-09-14, supports separate frames and pause-preserved playback; animation must never own gameplay damage. Aseprite is appropriate for packaging/canvas verification, not redrawing or inventing motion frames.

Builtin image generation prompt: stylized-concept game sprite atlas, 3×3 equal cells, same anime Vanguard identity as supplied reference, waist-up including both hands, dark steel armor/gold trim/teal scarf, three-quarter facing right. Row1 sword preparation/slash follow-through/recovery, row2 guarded preparation/shield brace/recovery, row3 restorative preparation/open-palm blue-white casting/recovery. Consistent camera/scale/bottom anchor, clear silhouettes, no text/borders/labels/scenery, generous key-color gutters, solid pure green chroma-key background absent from character and effects. No green light/spill, no checkerboard, no extra characters, no pose clipping. Candidate scope only; verify output before runtime promotion.
