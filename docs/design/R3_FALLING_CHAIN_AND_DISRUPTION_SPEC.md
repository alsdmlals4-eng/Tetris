# R3 변경 설계 명세 · 낙하 연결 퍼즐과 적 보드 파괴

## 현행 추가 결정 - 2026-09-21 테트리스 기술 보급과 적 대응 패턴

승인된 권장안의 구현 범위다. 실제 진입은 `scenes/replanned_r3/resource_choice.tscn`, `use_mastery=true`. `mastery_session.gd`가 기존 assist/finisher를 확장하고 `data/replanned_r3/mastery.json`이 추가 수치를 소유한다. 실행·남은 필수 작업은 `docs/operations/MASTERY_PATTERNS_EXECUTION.json`, 실제 검증은 `docs/validation/mastery-patterns-20260921/`에서 확인한다. 아래보다 오래된 날짜의 절은 해당 버전 이력이다.

### 기술을 보급으로 연결

공격·방어·치유·시계 타일의 기존 개별 효과는 유지한다. 추가 기술 점수는 **뿌요 공급 진행도에만** 합산한다. 가짜 소거 타일을 만들어 공격력이나 시간을 추가 지급하지 않는다.

| 조건 | 추가 공급 단위 | 끊김·상한 |
| --- | ---: | --- |
| I로 한 번에 4줄 클리어 | 10 | I의 1~3줄은 일반 소거 |
| T스핀 1 / 2 / 3줄 | 5 / 10 / 15 | T, 마지막 성공 동작 회전, 피벗 주변 4모서리 중 3개 점유 |
| 연속 소거 2 / 3 / 4회 이상 | 2 / 4 / 6 | 소거 없는 고정으로 연속 소거만 초기화 |
| 어려운 소거 연속(B2B) | 5 | 4줄 또는 T스핀 소거. 무소거는 유지, 일반 1~3줄 소거는 끊김 |
| 같은 배치에서 조건 중첩 | 합산, 최대20 | 무소거 T스핀은 판정 표시만, 보상0 |

10 공급 단위 → 3쌍, 대기 최대10쌍, 초과1쌍 → 보너스 마나1. 예: 첫 4줄은 실제40칸 + 기술10단위 = 15쌍을 획득한다. 보관 공간을 채우고 나머지는 보너스가 된다. 적 파괴는 공급·스킬·콤보를 생성하거나 초기화하지 않는다. 일시정지·보드 전환은 기술 상태를 유지한다. 모든 수치는 승인된 시험값이며 최종 밸런스는 아니다.

### 적별 정체성과 대응

기존 4행동 주기와 모든 적의 파괴 최소1회를 유지한다. 파괴 예약은 기존 고정 셀 ID를 사용하며 자원 보상0이다. 효과는 행동 연출의 실제 접촉 시점에 한 번만 적용한다.

| 적 / 행동 인덱스(0부터) | 새 패턴 | 플레이어 대응·피드백 |
| --- | --- | --- |
| 외곽 균열 / 1 | 기존 강타 후 약점 노출8초, 다음 공격 스킬 최종 위력 +25%, 한 번 | 공유 시뮬레이션 시간으로 표시. 공격 자원까지 합산 후25% 올림; 방어막 흡수 전에 적용 |
| 주조소 / 0 | 직접 피해 대신 균열 장갑12. 중첩 없음, 다음 적 행동 접촉 종료 때 만료 | 적 장갑 잔량과 만료 안내. 공격으로 먼저 소진하거나 다음 행동까지 준비 |
| 감시탑 / 1 | 직접 HP 피해 대신 방어도 최대6 제거 | 스킬 보호막 자체는 제거하지 않음. 기존 보호막의 행동 대상 종료 규칙은 유지 |
| 균열의 핵 / 1 | 기존 강타35 + 차징 추가10 | 해당 준비 시간에 실제 적 HP 피해 누적20이면 추가10만 취소, 기본35는 유지. 장갑 흡수량은 피해 누적에 제외 |

현재/다음 패턴, 예상 총피해와 방어 후 HP 피해, 장갑·약점·차징 누적을 표시한다. 약점 시간은 일시정지·보정 선택·스킬/적 연출 동안 공유 시뮬레이션과 함께 멈춘다. 약점 만료와 보드 정산이 같은 시점이면 먼저 시간을 소모하므로 만료 후 보너스는 없다. 전투 패배 시 더는 쓸 수 없는 적 임시 상태를 정리하고 저장 가능하게 한다. 입력 반전·시야 차단·보관쌍 강탈은 넣지 않는다.

### 연습·저장·검증 연결

전투 준비에 4줄, T스핀 더블, 연속 소거의 짧은 연습을 추가했다. 실제 회전/낙하 규칙과 보급 계산을 사용하되 시간 압박 없이 입력을 기다린다. 연습 결과는 저장·전투로 옮기지 않으며 기존 뿌요2연쇄 연습도 유지한다. 실패하면 연습 종료 후 재진입할 수 있다.

새 저장은 `user://mastery_patterns/`, `r3-mastery-v1` / `r3-mastery-choice-v1`. 이전 assist/finisher/R3 저장 경로와 해시를 덮어쓰거나 자동 마이그레이션하지 않는다. 디스크 검증기는 구형·신형을 각각 검증한다. 실제 타일 원장, 별도 기술 보급 원장, 배치 이력의 일치와 적 패턴 문맥을 확인한다. 공유 레이어에는 기본 동작이 동일한 좁은 팩토리·검증 훅만 추가한다.

경험 가설: 어려운 LINE 조작이 더 많은 뿌요 기회로 이어지고, 적의 준비와 취약 구간이 자원/공격 선택을 의미 있게 만든다. 반례: 기술 보상이 초보자의 격차만 벌리거나 적 정보가 많아 대응을 못 이해하면 실패다. MACHINE은 중복·상한·저장·타이밍을, RUNTIME은 표시와 접촉을, HUMAN은 기술 보상 인지·패턴 대응·재시도 의사를 구분한다.

공급 비교는 seed42, 완화 난이도, 외곽 균열, 동일30초 scripted 구간 / 1.5초마다 결정 / 뿌요 소비 없음으로 기록했다. LINE 기본60·기술0·초과12, SWAP 기본57·기술0·초과9, 두 모드 실제 시뮬레이션28.4초. LINE은 낮은 구멍/높이 선택, SWAP은 첫 합법 수 선택이므로 **사람의 속도·공정성·재미 증거가 아니다**. 기술 지급은 별도의 실제 입력 연습으로 검증한다. 다음 사람 관찰에서는 동일 적·설정에서 초보/숙련을 나눠 단위/분과 연쇄 기회·재미를 기록한다.

기본 production 진입·R2·기존 캠페인·엔진·승인 이미지·플러그인·전역 설정은 보호한다. 캠페인/메인메뉴 통합, 결과 화면 확대, 최종 밸런스·아트·출시는 별도 범위다.

## 최신 추가 결정 - 2026-09-20 보너스 보정과 티어 보상

사용자 승인 범위: 모든 적의 파괴 행동, 대기 뿌요 10쌍과 초과 보너스, 초보자 보정/연습, 티어 위력/연출. 실제 consumer는 `scenes/replanned_r3/resource_choice.tscn`의 `use_assists=true`다. 아래 절은 해당 진입점에서만 이전 위력/보관 한도를 대체한다. 기본 production·R2·기존 R3/finisher 저장은 바꾸지 않는다. 코드/수치 owner는 `src/replanned_r3/r3_assist_session.gd`와 `data/replanned_r3/assist.json`, 실행 계획·검토는 `docs/operations/BONUS_ASSIST_EXECUTION.json`이다.

### 역할과 자원 보존

자원 확보 = 선택한 LINE 또는 SWAP. 스킬 사용 = 고정 낙하쌍 CHAIN. 첫 소거 문양이 스킬 종류를 고정하고 연쇄가 끝나면 한 번만 발동한다. 네 문양/4개 연결/공유 타이머는 그대로다.

- 대기 보급은 최대 10쌍, 이미 낙하 중인 한 쌍은 별도다. 초기 4쌍, 정규화 자원 10단위당 3쌍은 유지한다. LINE/SWAP의 기존 속도 보정 계수를 이 변경에서 다시 바꾸지 않는다.
- 수령할 때 빈 보관 칸을 먼저 채우고 **넘친 쌍당 보너스 마나 1**을 얻는다. 나중에 보급을 소비해도 이미 번 보너스는 사라지지 않는다. `supply.discarded`는 구형 공통 원장의 필드명이며, 새 프로필에서는 소멸량이 아니라 누적 초과 획득량으로 읽는다. 잔액 = 초과 획득량 - 검증된 사용 내역.
- CHAIN 소거·스킬·적 파괴는 보너스나 뿌요 보급을 재생산하지 않는다. 일반 마나 전액을 임의 환전하지 않는다.

| 보너스 사용 | 비용 | 결과 / 실패 처리 |
| --- | ---: | --- |
| 고정 뿌요 문양 변경 | 2 | 선택한 1개를 A/D/H/T로 변경. 동일 문양은 무료 거절 |
| 고정 뿌요 좌우 보정 | 1 | 인접 칸과 교환, 빈 칸이면 이동 후 중력 적용. 보드 밖은 무료 거절 |
| 긴급 치유 | 2 | HP +5, 최대 100. 이미 가득 차면 무료 거절 |
| 다음 공격 강화 | 2 | 공격 가산 +5. 즉시 적 피해를 주지 않고 다음 공격 접촉에 합산 |
| 긴급 방어 | 2 | 방어도 +5 |

보정 요청은 떨어지는 쌍·연쇄·행동 연출이 끝나는 안전 경계까지 예약된다. 이미 확정된 적 행동은 먼저 실행한다. 보정 창이 열린 동안 보드와 적 ETA 모두 정지한다. 잘못된 대상/금액 부족/취소는 보드·잔액을 바꾸지 않는다. 보정 결과 4개가 연결되면 선택한 셀을 축 기준으로 기존 시동 판정 → 연쇄 → 단 한 번 스킬을 실행한다. 선택 중 저장은 막고 정상 안정 경계에서만 저장한다. 새 저장은 `user://bonus_assist/`, schema `r3-assist-v1` / `r3-assist-choice-v1`로 격리한다.

### 보상과 표현

| 티어 | 공격 | 방어 | 치유 | 시간(초) | 연출(초) |
| --- | ---: | ---: | ---: | ---: | ---: |
| T1 | 8 | 6 | 5 | 0.5 | 1.4 |
| T2 | 18 | 10 | 9 | 1.0 | 1.5 |
| T3 | 32 | 16 | 15 | 1.5 | 1.7 |
| T4 | 50 | 24 | 23 | 2.0 | 2.0 |
| T5 | 74 | 34 | 33 | 2.5 | 2.2 |
| T6 | 104 | 46 | 45 | 3.0 | 2.5 |

공격은 준비한 가산을 한 번만 더한다. 방어는 기존 보호/피해 행동 제한을 유지하며 긴급 방어도와 구별한다. 시간은 자원 보너스와 합쳐 현재 적 행동당 최대 3초다. 7연쇄부터 표시는 T6이며 공격 +30/파동, 치유 +12/파동, 방어/시간은 T6 상한이다. 수치는 플레이 시험용이며 최종 밸런스 승인 아님.

기존 승인 캐릭터·아이콘을 재사용한다. 티어마다 링 수 1~6, 광선 수 4~24, 효과 크기와 음높이를 변화시키고 T3부터 궤적, T5부터 외곽 문양을 더한다. 타이머·피해 적용은 표현과 분리한다. 적 행동도 기존 1.6초 연출 동안 양쪽 시간을 멈춘다. 효과음 끄기/동작 줄이기를 제공한다.

### 모든 적의 파괴와 학습

| 적 | 4행동 주기 중 파괴 위치(1부터) | 표준 파괴 수 |
| --- | ---: | ---: |
| 외곽 균열 | 2 | 1 |
| 감시탑 | 2 | 2 |
| 주조소 | 3 | 3 |
| 균열의 핵 | 2 | 4 |

여유 모드는 1개 감소하되 최소 1개다. 파괴 대상은 기존 고정 ID 예약, 2초 예고와 무보상 적 정산을 재사용한다. 적마다 최대 수만큼 **존재하는 고정 셀**을 파괴하며 빈 보드에 가짜 파괴를 만들지 않는다. 선택 화면에서 4종 적을 직접 시험할 수 있다.

무료 학습: 현재 착지 시 소거 연결 수, 첫 문양/연쇄/1회 발동 설명, 실제 명령으로 공격→치유 2연쇄를 만드는 짧은 연습. 연습은 적 공격·저장이 없고 실전 진행을 덮어쓰지 않는다. 연결 수 표시는 현재 배치 예고이지 미래 정답 탐색이 아니다.

### 재미 가설과 검증 경계

가설: 초과 자원을 배치 수정에 사용할 수 있으면 초보자가 실패를 만회하면서 자원/스킬 보드의 역할을 이해하고, 긴 연쇄의 큰 보상이 위험 감수를 유도한다. 반례: 자원 보드만 반복해 긴급 효과로 버티거나 보정이 필수 노동이 되는 경우, 높은 티어가 전투를 단번에 끝내 선택을 줄이는 경우. 자동 검사는 자원 보존·타이밍·한 번 발동·저장을 확인한다. 실제 렌더는 배치·표시를 확인한다. **초보자 2연쇄 성공률, 보정 사용 이유, 자원 모드별 보너스/분, 전투 소요 시간, T3+ 도전 빈도는 HUMAN: NOT_RUN**이며 후속 플레이 관찰에서 측정한다. 리스크가 낮다는 이전 표현을 보상 부족의 근거로 자동 일반화하지 않고 체감 관찰을 별도로 받는다.

비교 근거: 기존 R3 supply/finisher/correction 재사용, SEGA 공식 Puyo Puyo Champions의 단계별 레슨/연습 방식을 짧은 독립 연습으로 ADAPT. 3개 연결로 변경하거나 적 타이머만 느리게 하는 방식은 코어/공유 시간 불일치 때문에 REJECT. 외부 사례는 재미를 입증한 자체 플레이 자료가 아니다.

## 현재 추가 결정 — 2026-09-20 시동 문양과 연쇄 완성 스킬

사용자의 플레이 후 피드백과 이어진 권장안 승인을 반영한다. **선택형 자원 전투에서만** 아래 규칙이 이전 수동 ATK/DEF/SUP 선택과 파동별 발동을 대체한다. 자원 확보 LINE/SWAP 선택, 유한 낙하쌍 보급, 문양별 자원 효과와 승인 자산은 유지한다. 이전 R3/원정 실행은 호환성 경로이며 새 규칙을 적용했다고 표시하지 않는다.

- 첫 플레이어 소거의 시동 A/D/H/T가 공격/방어/치유/시간 스킬을 결정한다. 같은 첫 파동에 여러 문양이 터지면 이번 낙하쌍의 회전축 셀을 포함한 소거 → 문양별 소거 수 최다 → 가장 위 행, 같은 행은 왼쪽 셀 순으로 판정한다. 배열 순서·프레임 속도와 무관하다. 현재 착지로 시작할 문양을 예고한다.
- 이후 파동은 시동을 바꾸지 않으며 연쇄 전체가 안정화된 뒤 **단 한 번** 발동한다. 적 파괴가 만든 정산은 시동·콤보·스킬을 만들지 않는다. 적 파괴로 플레이어 연쇄가 끊겨도 이미 완료한 플레이어 파동은 보존하고, 적 정산까지 안정화된 후 해당 파동만 한 번 계산한다. 패배 시 미발동 기술은 취소한다.
- 공격/치유는 기존 파동별 기본 위력을 합한다(공격 3연쇄=4+6+8=18). 공격 자원은 접촉 시점에 한 번만 소모한다. 방어는 마지막 단계의 최대 보호 수치와 현재 피해 행동 대상 제한을 유지한다. 시간은 T1~T6=0.25/0.5/0.75/1/1.25/1.5초이며 자원 시간 보너스와 **현재 적 행동당 총 3초 상한**을 공유한다. 확정된 공격을 취소하거나 다음 행동으로 잔여를 넘기지 않는다.
- 연쇄 정산 자체는 기존 공유 시간 흐름을 유지한다. 연쇄 완성 후 플레이어 연출 1.4초(4연쇄 이상 2초), 적 행동 연출 1.6초. 플레이어 접촉 0.5초/적 접촉 0.6초에 실제 효과 1회 적용. 연출 동안 **양쪽 보드·공유 ETA·보급 조작 모두 정지**한다. 적과 연쇄가 같은 순간이면 적 확정 행동이 먼저다. 이 정지는 시간 문양 보상이 아니다. 일시정지/포커스 상실은 연출 시간도 멈춘다.
- UI는 수동 스킬 버튼을 제거하고 시동 예고/확정, T1~T6 진행, 합산 위력, 최근 발동과 연출 중 양쪽 정지를 표시한다. 시간 기술은 기존 지원 캐릭터 포즈와 시간 문양을 재사용한다. 새 이미지/최종 자산 승인을 주장하지 않는다.
- owner: `data/replanned_r3/finisher.json` → `r3_finisher.gd`(선택·산술), `r3_finisher_session.gd`(순서·접촉·검증), `resource_choice.tscn`/`r3_screen.gd`(실제 화면). 기존 `r3_session.gd`는 기본 규칙을 보존하는 작은 확장 지점만 제공한다.
- 저장은 `user://starter_finisher/save.json`, schema `r3-finisher-v1`/`r3-finisher-choice-v1`. 기존 `user://resource_choice/`, R2, legacy R3는 덮거나 암묵 변환하지 않는다. 연쇄/연출 중 저장 불가; 안정화 후 소거 원장·발동 이력·규칙 hash를 교차 확인하고 복원해도 재발동하지 않는다.

재미 가설: 별도 메뉴 선택 없이 놓는 문양이 전투 선택이 되고, 연쇄 완성의 한 번 큰 기술이 읽히며 보람을 준다. 반례: 첫 문양을 예측하지 못함, 혼합 소거 결과가 임의적으로 보임, 긴 연출로 반복 피로, 시간/치유 시동만 강요됨. 기계 검증(결정성·중복·경계·저장), 실제 화면 검증(예고/연출/ETA), 사람 플레이 관찰(의도 설명·속도·반복 피로)을 구분한다. 사람 재미/수치 최종 검수는 **NOT_RUN**. 비교한 대안은 파동별 발동 유지 REJECT(최신 지시와 충돌), 마지막 단계 수치만 지급 REJECT(공격·치유 암묵 하향), 누적 공격·치유/최대 방어 ADAPT(선택). 진행/증거: `docs/operations/STARTER_FINISHER_EXECUTION.json`.

## 현재 추가 결정 — 2026-09-20 자원 퍼즐 선택

사용자가 자원 확보를 **테트리스 또는 타일 교체 중 선택**, 스킬 발동을 **뿌요형 낙하 연쇄로 고정**하도록 변경하고 권장안을 승인했다. 이어 PR118의 필요한 구현을 최신 main 기반 새 작업 브랜치로 선별 통합하도록 명시 승인했다. PR117/118 자체의 변경·병합·종료는 승인 대상이 아니다. 아래 9/14 문서의 '교환 게임 제거', 'LINE만 보급'은 해당 버전의 기록이며 이번 선택형 규칙에는 적용하지 않는다.

- 실행 consumer: `scenes/replanned_r3/resource_choice.tscn` → `r3_screen.gd` → `r3_session.gd`. 기존 production 기본 실행과 R2 실행은 보존한다.
- 전투 준비에서 선택하고 전투 동안 고정. 전투 종료 뒤 다시 선택 가능. 설정은 `user://resource_choice/preference.cfg`; 진행은 별도 `user://resource_choice/save.json`. R2/기존 R3 저장을 덮어쓰지 않는다.
- 내부 `LINE` workspace는 선택한 자원 보드를 뜻한다. `resource_mode=LINE|SWAP`가 실제 보드를 결정한다. `CHAIN`은 항상 낙하쌍이다. 세 번째 보드를 몰래 시뮬레이션하거나 전환으로 재생성하지 않는다.
- SWAP은 기존 R2의 8×8 상하좌우 교환/가로세로 3매치/300ms 파동/무효 교환 원복/수 없음 재생성 규칙을 재사용한다. 보상·자동 스킬 consumer는 재사용하지 않는다. 모든 플레이어 자동 연속 소거는 자원만 지급한다.
- 보상 환산 owner: `data/replanned_r3/resource_choice.json`. 초기 비교값은 같은 문양 2셀마다 자원 1단위, 문양별 나머지를 저장한다. 공격/방어/치유/시간 의미와 기존 시간 연장 상한은 R3/R2 전투 owner를 재사용한다. 환산된 10단위마다 기존 보급 모듈이 3쌍 지급한다. 이는 **TRIAL_NOT_HUMAN_VERIFIED**, 동등한 실제 효율을 입증한 값이 아니다.
- `resource_swap.gd`: 보드·개별 셀 ID·파괴/보충. `resource_rewards.gd`: 실제 소거 ID의 중복 검증·분수 누적·지급 단위. `r3_session.gd`: 자원/보급 원자적 정산, 유일한 낙하쌍 스킬 consumer. UI는 직접 피해나 보급을 계산하지 않는다.
- 적 파괴는 일반 셀 ID만 예약하며 활성 퍼즐 종류를 바꾸지 않는다. SWAP에서는 현재 cascade가 안정화될 때 파괴와 후속 정산을 무보상 처리한다. HP 피해/공유 ETA는 기다리지 않는다. 파괴로 생성된 자동 매치는 자원·보급·스킬을 주지 않는다.
- 기존 LINE snapshot `r3-session-v1` 유지. SWAP만 `r3-session-choice-v1` 확장으로 보드·환산 원장을 포함. 저장 복원은 원장-보드 교차 검증과 데이터 hash 검증 후 적용한다. 거절된 명령/복원은 기존 상태 보존. 준비 화면에서는 저장을 금지하여 시작 전 기본 상태가 기존 전투를 덮지 못하게 한다.
- 화면은 좌측 보드 하나, 우측 적/공유 ETA/Current·Next/플레이어/스킬 유지. SWAP 클릭 두 칸 또는 방향키+Enter/Space; Tab 전환. 기존 뿌요·테트리스 조작과 Space 포커스 방어 유지. 실제 최종 UI/아트 확정이 아니라 기존 자산을 유지한 기능 연결이다.

### 재미·밸런스 검증 연결

의도: 선택한 자원 퍼즐의 손맛을 유지하면서 자원 준비가 낙하 연쇄의 스킬 발동으로 연결됨. 반례: SWAP만으로 승리, 한쪽만 일방적으로 우세, 보급이 반복 숙제, 어떤 소거가 스킬을 내는지 오해.

기계 검사: 소거 원장 중복/분수 누적/10단위 보급/적 무보상/CHAIN 전용 발동/저장·pause·전환. 화면 검사: 선택 상태, 보드 한 개, 같은 ETA, 자원 안내와 실제 보상, 재개/실패 복귀. 사람 관찰은 기존 `TETRIS_R2_PLAYTEST_RECORDS.md` 절차를 버전 구분하여 재사용한다. 동일 적·난이도·규칙 hash 조건에서 첫 자원/첫 기술까지 시간, 자원 보드 활성 시간과 전투 총시간당 수급, 누적/순간 보급, 피해·처치·반복 피로를 초보/숙련 별로 관찰한다. 같은 숫자 seed라도 두 퍼즐 난이도가 같다는 가정은 금지한다. HUMAN: NOT_RUN.

검토 대안: 강제 교환 대기시간 REJECT(조작감 저하), 보상 환산 ADAPT(선택), 모드별 적 수치 분리 DEFER(비교/유지 비용). 퇴행 시 이번 선택형 변경만 되돌리고 R3/R2 저장·자산·원본 PR은 유지한다. 실행·검증 상태는 `docs/operations/RESOURCE_CHOICE_EXECUTION.json`과 `docs/validation/resource-choice-20260920/README.md`에서 확인한다.

---

## 2026-09-14 승인 설계 기록

작성: 2026-09-14. 상태: **사용자 변경 방향 승인 / 상세 권장안 검토용 / 미구현**.
후속 승인2026-09-14: 사용자 `좋아 권장안대로 작업진행해`로 본 권장안의 구현을 승인했다. 아래 상세안은 승인된 시험값이며 최종 밸런스/Human 승인과 구분한다. 구현 진행은 연결된 계획에서 추적한다.
현재 실행 기준: main `69f4f591e038b4912d9761bf943aefd986170ace`.
이번 요청은 남은 작업 및 설계·구현 명세 준비다. 아래 초깃값을 승인된 밸런스나 현재 동작으로 표시하지 않는다.
상세 구현 순서: [R3 구현 계획](../superpowers/plans/2026-09-14-r3-falling-chain-disruption.md).

## 1. 권위와 유지 범위

사용자 변경: (U1) 적 공격에 플레이어 보드의 블록 몇 개를 무작위로 파괴하는 패턴 추가. (U2) 두 번째 블록 교환 게임을 뿌요뿌요식으로 교체. 이유: 두 번째 게임만 하는 것이 지나치게 유리함.

U1/U2는 변경 방향이다. 아래 보급량·회전 규칙·파괴 방어 방식은 AI 권장 상세안이며 **RECOMMENDED_NOT_BALANCE_LOCKED**다. 현재 R2 구현/자산/PDF는 보존한다. 기존 8×8 교환 명세가 새 설계의 제약은 아니지만, 새 구현이 검증되기 전 현재 실행 파일을 소급해 R3라고 부르지 않는다. Base9.4.4 유지. 열려 있는 다른 draft PR은 읽기 전용.

유지: 좌측 단일 보드/우측 전투 50:50, LINE↔CHAIN 자율 전환, 같은 적 ETA를 표시하는 공유 행동 타이머, 공격·방어·치유·시간 네 문양, LINE 직접 자원 보상, ATK/DEF/SUP 선택, 연쇄 파동별 자동 발동, T1~T6 설명/미리보기, 전체 pause, 원정 분기/보급/결말, 사용 중인 원본 이미지와 사운드.
제외: 교대 턴, 시간에 따른 강제 보드 전환, 상대 플레이어/PvP, 원작 캐릭터·로고·소리·화면 복제, 장비/상점/새 직업 확장.

## 2. 현재 문제의 확인 수준

`r2_chain.gd::commit_wave`는 소거 빈칸을 새 문양으로 채우고 다음 매치를 계속 처리한다. `r2_session.gd::command(chain_swap)`에는 LINE에서 확보한 공급량을 요구하는 조건이 없다. 따라서 CHAIN 자체가 스킬 발동 기회를 계속 공급한다. 실제 코드로 확인한 구조적 문제다.
현재 원정 테스트 드라이버 또한 LINE 줄 소거 없이 CHAIN 명령으로 승리 경로를 만든다. 다만 이 드라이버는 생각 시간이 0인 합법 수 탐색이므로 **인간의 승률·초당 피해량 우월성이 실측됐다는 증거는 아니다**. 사용자 체감과 코드상 무한 발동 가능성이 방향 변경의 근거이며, 정량 우월도는 새 비교 시험으로 측정한다.

| 대안 | 장점 | 문제/장기 비용 | 판정 |
|---|---|---|---|
| A 교환 보드 유지, 스킬 수치만 약화 | 저비용 | 사용자 변경과 다름. 무한 공급과 역할 중복 유지 | REJECT |
| B 낙하 연결로 변경, 낙하쌍 무한 공급 | 직접 연쇄를 설계하는 재미, 새 재화 없음 | 숙련 CHAIN 단독 전략을 구조적으로 막지 못함 | 비교군으로 보존 |
| C 낙하 연결 + LINE 소거로 낙하쌍 보급 | LINE 준비와 CHAIN 실행이 실제 의존 관계 | 보급 대기/강제 숙제 위험, 수치 검증 필요 | 권장 상세안 |

공급 횟수는 다섯 번째 타일 종류나 구매 재화가 아니라 **다음에 놓을 수 있는 조각 수**다. 숨기지 말고 NEXT 근처에 표시한다. 시간제 강제 전환이나 스킬 전면 쿨다운은 추가하지 않는다.

## 3. 조사·벤치마킹 적용

2026-09-14 공식 원출처를 읽었다. 직접 플레이/개발자 인터뷰를 한 것은 아니다.

| 원출처 | 확인한 패턴 | 우리 적용 |
|---|---|---|
| [SEGA 게임 설명](https://www.sega.jp/history/arcade/product/9156/) | 6열, 두 개 한 쌍, 동색4개 이상, 단계적 소거 | ADAPT 낙하/연결 핵심. 우리 네 문양·보스전·자동 기술로 연결 |
| [SEGA 공식 레슨](https://puyo.sega.jp/puyopuyotetris/mode/lesson/index.html) | 같은 색 연결, 소거 후 연쇄, 동시 소거와 연쇄 구분, 계단/끼워 쌓기 | ADAPT 학습 순서와 파동 구분. 원작 그림/명칭/점수식은 사용하지 않음 |
| [공식 Swap 설명](https://puyo.sega.com/tetris/rule/swap/index.html) | 타이머에 의한 두 보드 전환 | REJECT 강제 전환. 우리 공유 타이머는 적 ETA이며 자율 전환 유지 |
| [제작자 칼럼](https://puyo.sega.jp/puyopuyo!/producer/004/) | 예고/상쇄가 대응 선택을 만듦; 방해 방식에 따라 플레이가 달라짐 | ADAPT 사전 예고와 대응. 원작 방해물 전송/점수 상쇄는 복제하지 않음 |
| [Godot Node 생명주기](https://docs.godotengine.org/en/stable/classes/class_node.html), [저장 경로](https://docs.godotengine.org/en/stable/tutorials/io/data_paths.html) | 트리에 추가될 때 초기화, user://는 실제 저장 공간 | REUSE 초기화 전 검증 경로 분리. 화면 속성 변경만으로 저장 격리를 추정하지 않음 |

독창성의 후보는 단순히 두 퍼즐을 번갈아 하는 것이 아니라 **LINE에서 방어/회복/시간과 배치 기회를 준비하고, 연결 퍼즐에서 적의 예고를 보며 그 준비를 기술로 바꾸는 구조**다. 독점적 새 메커닉·법적 안전성은 주장하지 않는다. 상업 출시 전 원작 표시/자산/유사성 검토는 별도 gate.

## 4. R3-CHAIN: 낙하 연결 보드

### 보드와 조작

- 6열×12가시행+숨김2행. 빈칸은 EMPTY, 색은 A/D/H/T. 초기 보드는 비어 있음.
- 한 조각은 축과 위성 두 셀. 축 spawn=(2,-1), 위성=(2,-2); 가시 좌표 y=0..11, 숨김=-2,-1.
- 좌/우 이동, 아래 soft drop, 시계/반시계 90도 회전, 즉시 낙하. LINE과 공통 키 재사용. CHAIN의 HOLD는 첫 범위에서 없음; NEXT는 다음 두 쌍 표시.
- 90도 회전의 후보 offset 순서 (0,0),(-1,0),(1,0),(0,-1). 처음 충돌하지 않는 후보 적용, 전부 충돌이면 무효. 180도/원작 퀵턴은 미포함. 천장 밖 회전 불가.
- 활성 중 중력은 700ms당1행, soft drop50ms당1행, 접지 확정500ms, 이동/회전 잠금 리셋 최대8회. 성공한 이동/회전만 리셋. hard drop은 즉시 확정. 모두 시험값.
- 두 셀이 착지한 뒤 각 열 안에서 아래로 정렬한다. 수평 조각 한쪽 밑이 비어 있으면 해당 셀만 떨어진다. 그 이후 연결 판정.
- 같은 문양이 상하좌우로 연결된 집합4개 이상 소거. 직선일 필요 없음(L/사각형도 성립). 대각선은 연결하지 않음.
- 같은 검사에서 성립한 모든 집합은 하나의 파동. 예: A4와D4 동시 소거도 C1/스킬1회.
- 소거 후 남은 셀만 아래로 낙하. **빈칸에 자동 보충하지 않는다.** 다음 낙하쌍만 새 셀의 공급원.
- 한 파동300ms(소거 예고180+낙하120)의 고정 논리 시간. 낙하 높이·렌더 FPS·효과 축소에 따라 발동 시점을 바꾸지 않음. 최대 연쇄21회 안전상한(총84셀/4); 초과 상태는 에러로 정지하고 진단하며 자동 보상/재생성 금지. 매 파동 최소4셀 감소와 신규보충0도 검사한다.
- 안정화 후 숨김행에 셀이 남거나 다음 spawn 두 칸이 막혀 있으면 CHAIN 넘침: HP25 직접 손실, CHAIN 보드만 비움. 낙하쌍·공격 bank·LINE 배치는 환급/리필하지 않음. 실제 HP0이면 패배. 판정은 소거가 살릴 기회를 준 뒤 한 번만.

### 전환·시간·선택

- 활성 낙하쌍 중 전환 가능. 위치/방향/중력·접지 잔여시간 보존, 비활성 보드는 낙하하지 않음. 숨겨 두어도 적 ETA는 진행.
- 연쇄 해소 중 전환은 기존 R2처럼 안정화까지 예약. 선택 계열은 **착지 확정 시** 저장하고 그 착지에서 이어지는 모든 파동에 동일 적용.
- 떨어지는 중에는 계열 변경 가능. 파동 시작 후에는 변경 불가. T단계 클릭은 설명만, 계열/배치/공급을 바꾸지 않음.
- 논리 상태: NEED_PAIR → FALLING → LOCKED_SETTLE → CLEAR_PENDING → FALL_SETTLE → (CLEAR_PENDING 또는 NEED_PAIR). DISRUPTION_SETTLE은 별도 원인 상태. PAUSED는 직교 flag.
- 다음 pair를 spawn할 때 공급1쌍 소비. spawn 불가 시 공급 차감 전에 넘침 판정. 이미 spawn된 pair는 전환/저장/피격으로 다시 차감하지 않음.

### 스킬

R2 ATK[4,6,8,11,14,18], DEF[3,5,7,10,13,17], SUP[2,3,4,5,7,9]를 첫 비교의 기준값으로 유지. 새 퍼즐 때문에 이후 재조정 가능하나 처음부터 공급·스킬·보스 HP를 모두 바꾸지 않는다.
T=min(파동번호,6). C7 이후 T6 유지. 안정화 시 연쇄 깊이0. 네 색은 CHAIN에서 연결 조건일 뿐 직접 자원 보상은 없음.
ATK는 단계값+준비 bank를 한 번 적용하고 bank0. DEF는 현재 미확정 적 행동의 방벽 목표치 max 갱신. SUP는 HP 상한까지 회복. 잘못된 입력·스폰·단순 낙하·적 파괴는 자동 스킬을 만들지 않음.

## 5. R3-SUPPLY: LINE↔CHAIN 연결 권장안

초기 잔여4쌍, 최대12쌍. 실제 플레이어 LINE 소거의 고유 셀10개당+3쌍(정상 1줄10개). 동시에4줄이면+12쌍, 저장 상한 초과는 버림/명시. 향후 부분 소거를 대비해 누적 remainder0..9를 저장하되 적 파괴는 이 누적에 넣지 않는다.
LINE 기존 보상 A→다음 공격+1, D→방어도+1, H→HP+1, T→현재 미확정 ETA+0.25초(행동당+3초)는 그대로다. 공급량은 문양과 무관하므로 특정 색을 기다릴 필요 없음.
공급0이고 활성 pair 없음: CHAIN 입력 배치만 비활성, '낙하쌍 0 · LINE 한 줄로 3쌍 보급' 표시. 강제 전환하지 않고 LINE 버튼을 강조. 적 ETA는 진행한다. 자원 공급이 없는 상태에서 무료 pair/무료 skill/시간 정지를 주지 않는다.
색 생성은 A,D,H,T 각2개의8셀 bag을 섞어 두 셀씩 묶는다. 공급을 얻을 때가 아니라 NEXT 큐 생성 시 색을 결정. 큐·bag·RNG 상태 저장. 전환/재개/보급 상한으로 재추첨하지 않음. 초깃값은 불리한 확률 보정이 없는 균등 분포다.
전투 종료 후 남은 공급과 CHAIN 배치는 이월하지 않음. 다음 전투4쌍/빈 보드. 원정 HP/방어/공격 준비의 현재 이월 규칙은 유지.

예: 잔여2쌍, LINE에서 A6/D4 한 줄 → bank+6/방어도+4/잔여5쌍. CHAIN 다음 pair spawn →4쌍. 두 번 연쇄 ATK → 첫4+bank, 둘째6. 적이 D3개를 파괴해도 방어도·공급 모두0 증가.
비교 시험에서 보급 이동이 숙제처럼 느껴지면 1줄당3→4쌍 또는 cap12→16을 먼저 비교한다. 무료 자동 보충으로 회귀시키거나 별도 MP까지 추가하지 않는다. 초기4쌍만으로 모든 적을 잡지 못한다는 수학적 보장과 게임의 재미는 구분한다.

## 6. R3-DISRUPT: 적의 무작위 블록 파괴

### 표적과 예고

- 해당 행동이 Current가 되는 순간 활성 보드 ID를 고정. 이후 전환해도 대상 보드는 바뀌지 않음. 비활성 대상은 보드 탭에 표적 수 표시. 둘 다 파괴하지 않음.
- 마지막2초 진입 시 대상 보드의 **고정된 일반 셀**에서 중복 없이 n개를 무작위 선택. 활성 조각, 고스트, HOLD/NEXT, 이미 소거 확정된 셀은 제외. 대상 부족이면 실제 수만 선택. 0개면 보드 효과0, HP 공격은 정상 실행.
- 좌표가 아니라 불변 cell_id를 예약. 표식은 이동하는 셀 ID를 따라가고 플레이어가 먼저 소거한 셀은 빗나감. 다른 셀로 재선정하지 않음. 시간 연장 후에도 같은 표적.
- seed는 run_seed와 action_instance_id에서 별도 RNG stream 생성. LINE/CHAIN 조각 RNG를 소비하지 않음. pause/저장재개로 바뀌지 않음.
- Current 카드에 'HP 피해 / [대상 보드] n개 파괴 · 파괴 보상 없음'. 실제 선택 전에는 대상 보드/최대수만, 선택 후에는 확정 수와 표식. 소리와 색만으로 알리지 않음.

### 실제 처리

같은 시각: pause 요청 → 적 행동 commit/HP 피해 → 플레이어 사망 판정 → 살아 있으면 적 파괴 → 플레이어 착지/소거/스킬. 현재 파동의 소거 셀은 소거 예약 시 보호하여 이미 약속한 파동과 표적을 겹치지 않게 한다.
연쇄 도중 ETA가 끝나면 HP 피해는 지연하지 않음. 보드 파괴는 현재 파동의 원자적 처리 뒤에 적용하고 다음 파동 검사보다 앞선다. 보호 셀은 표적 선정에서 제외했으므로 중복 소거 없음. 대기하는 파괴 이벤트를 저장한다. 스킬로 적이 사망해도 이미 commit된 파괴 이벤트는 살아 있는 플레이어 보드에 한 번 적용; 미확정 행동만 취소.
파괴 이벤트 cause=ENEMY_DESTROY: 직접 자원0, 보급0, 스킬0, 점수0. 파괴로 새 연결이 생긴 후속 정리도 cause=ENEMY_SETTLE를 유지해 무보상. 플레이어 연쇄는 이 개입 경계에서 종료하며, 이전까지 확정된 스킬을 취소하지 않는다.
LINE에서는 셀만 빈칸으로 바꾸고 개별 셀 중력은 적용하지 않는다. 자연 소거로 가산하지 않는다. CHAIN에서는 생존 셀 낙하→4연결 무보상 정리를 안정화까지 진행. 상한/원인 구분 적용. 활성 낙하 pair는 파괴 정리 중 제자리·타이머 동결 후 계속(그동안 적 전투 시간은 정상). 새 자동 pair는 정리 뒤 한 번만 spawn.

### 방어와 역효과

일반 방어도/DEF 방벽은 **HP 피해만** 막고 파괴 개수는 줄이지 않는 첫 비교안. '방어로 완전히 막았다'라는 문구는 보드 파괴가 남으면 사용 금지. 대응은 예고 셀 선소거/연쇄 선발동/시간 연장/처치/다른 보드에 대비된 배치 사용이다. 파괴 전용 방패 스킬은 첫 범위에서 추가하지 않음.
블록을 없애면 높이가 낮아져 유리해질 수 있다. 따라서 모든 파괴를 항상 손해라고 주장하지 않는다. 사용자 요청대로 파괴를 유지하되 일반 피해와 함께 드물게 사용하고, 구축 중인 연결/LINE 채움의 손실을 노린다. '파괴 공격이 오히려 기대 보상'이라는 시험 결과가 나오면 대상 선택/빈도를 수정한다. 임의 자원 압수·방해 블록 추가로 몰래 의미를 바꾸지 않음.

### 첫 콘텐츠 시험값

| 적/행동 | 현재 수치 | R3 제안 | 목적 |
|---|---|---|---|
| 외곽 균열 | 12초/6,16초/16,12초/6,8초/0 | 파괴 없음 | 두 보드 보급·연쇄 학습 |
| 감시탑·파편 사격 | 7초/7 | 유지 | 일반 견제와 구분 |
| 감시탑·추적 사격 | 8초/8 | HP8 + 무작위2셀 | 작은 예고 방해 |
| 주조소·주조 붕괴 | 16초/26 | HP26 + 무작위3셀 | 느린 구축 파괴 |
| 균열 핵·균열 분쇄 | 14초/35 | HP35 + 무작위4셀 | 최종 종합 대응 |

다른 행동·적 HP는 기준값 유지. 완화 모드는 현재 피해/ETA 완화 규칙을 유지하고 파괴 수 max(1,n−1). 마지막2초 표적 예고는 더 줄이지 않음. 난이도 효과는 데이터와 계산된 예고가 일치해야 한다.

## 7. 데이터·저장·화면 계약

구현 후보 이름은 R3로 분리한다. `data/replanned_r3/rules.json`이 위 수치의 단일 기계 정본, 이 문서는 의미/AC 정본. 원작 이름은 내부 비교 문맥에서만; 화면은 'CHAIN · 낙하 연결' / 조각은 기존 세계관 문양의 '룬 쌍' 권장.
새 스냅샷 `r3-session-v1`: rule_pack_hash, run_id, seed, active_workspace, elapsed_us, selected_category, 기존 HP/bank/armor/ward, LINE상태, chain_state, supply_pairs/remainder, pending_disruptions, processed_event_ids.
chain_state: 6×14 고정 셀(cell_id,kind), 활성 pair(id,axis,satellite,orientation), gravity/lock 잔여, lock_reset_count, NEXT2/bag/RNG, phase, chain_id/wave_index/category_snapshot, 예약 소거 ID, 파동 잔여, cause. 처리 순서와 RNG stream version도 저장.
LINE도 문양 격자와 동일 크기의 셀 ID 격자를 가진다. R3 어댑터가 착지/줄 압축/부분 파괴를 원자적으로 동기화하고, 아래 줄이 소거되어 표적이 내려가도 ID는 유지한다. 전투별 namespace와 단조 증가 ID 카운터를 저장하며 제거된 ID는 재사용하지 않는다. 구형 R2 LINE 규칙의 재사용은 ID 추적까지 이미 구현됐다는 뜻이 아니다.
R2 저장을 R3 보드로 자동 변환하지 않음. 별도 `user://replanned_r3/`와 새 원정 envelope. R2 이어하기는 R2 실행 경로에서만 제공/보존, R3 메뉴에서 명시. 손상/다른 rule hash는 fail closed+백업 복원, 임의 초기화 금지. 안정 상태만 신규 체크포인트; 진행 중은 직전 안정 체크포인트 유지. pause 후 복원은 항상 paused.
같은 action/cell/clear/pair_spawn ID 중복 처리 금지. 공격 예약은 저장 시 원래 셀 ID와 연결, 재선정 금지. 모든 실패는 변화 전 스냅샷을 유지해야 함.

화면: 좌측 너비를 유지하고 보드6×12를 세로 중앙에 배치. NEXT2/잔여쌍/보급 안내는 하단에 두어 가로 공간 확보. 오른쪽은 큰 적·Current/공유 ETA/Next·얼굴 HUD·스킬 표시 유지. 드래그 교환/선택 커서 안내를 낙하/회전으로 완전히 교체. 대각 입력/스왑 강조·3매치 설명을 남기지 않음.
아이콘4문양은 기존 색/의미를 재사용하되 새 형태는 원작 얼굴 젤리 복제가 아닌 룬석. 신규 제작 전 consumer는 R3 battle Puzzle/ChainCells 및 ActivePair/NextPair,128px RGBA 원본/48~64px 목표 표시, 공통 pivot/padding. 상태는 normal/active/ghost/targeted/clear/hit. targeted는 색 외 균열 표식, clear/hit는 동일 원본 atlas/VFX. 승인 전 후보; 생성/승인/등록/런타임 상태 분리. 현재 이미지로 임시 기능 검증은 가능하나 최종 그림 완료가 아님.

## 8. 수용 시험(AC)과 재미 검증

| ID | 조건/동작 | 기대 결과 |
|---|---|---|
| AC01 | A가 L자로4개 / 대각으로4개 | 전자만 소거,1파동1스킬 |
| AC02 | A4,D4 동시 소거 후 H4 낙하 연결 | C1/C2,기술2회; 빈칸 자동보충0 |
| AC03 | 잔여2,LINE10셀 소거,1쌍 spawn | 잔여5→4,보상은 기존 문양별 합 |
| AC04 | 잔여0/활성없음 | 신규 배치 거부,LINE 전환 가능,ETA 정상 |
| AC05 | 잔여11에서2줄 소거 | 잔여12,초과5쌍 표시,재전송 증가0 |
| AC06 | 같은 pair 전환/재개 반복 | 색/위치/접지시간 동일,중복소비0 |
| AC07 | 파괴3표적 중1셀 선소거; 다른 표적은 아래 줄 소거로 이동 | 남은2의 현재 위치만 파괴,ID 유지,표적 재선정0,보상0 |
| AC08 | 파괴로 A4 연결 | 무보상 정리,스킬/공급/점수0 |
| AC09 | LINE중간 셀 파괴 | 구멍 유지,위 셀 개별낙하0,조각 충돌 정확 |
| AC10 | 같은 tick 치명 HP공격과 회복 파동 | 패배,회복/부활0 |
| AC11 | 파괴 예고 후전환/저장복원 | 같은보드/같은IDs/같은RNG결과 |
| AC12 | 스폰 막힘/숨김잔존 안정화 | HP25한번,CHAIN만초기화,보급추가지급0 |
| AC13 | 구형 저장을 R3로 로드 | 명시적 호환 거부,기존파일 동일 |
| AC14 | 100/125%글꼴,1280/1920,효과축소/무음 | 정보 잘림 없음; 같은 입력의 논리 이벤트 동일 |
| AC15 | 테스트실행 | 실제저장객체가 테스트경로일 때만 실행,일반저장 전후hash동일 |

자체 검토1: 초기 단순 교체안에 무한 공급 잔존, 파괴 보상·새 연결 발동, 전환 표적 회피, 저장 RNG 재추첨을 발견해 위 규칙으로 보완.
자체 검토2: 중간 파동 파괴 순서/선소거 빗나감/빈 보드/죽음 우선/넘침 환급/구형 저장을 재검토. 문서 차원의 모순 교정이며 자동·runtime·Human PASS가 아님.

비교: R2현행, R3무한쌍(B), R3보급쌍(C)을 동일 모드/적스케줄/스킬표/사고시간 포함한 시험으로 비교. 먼저20 seed의 재현 가능한 봇 스모크(즉시판단 봇은 밸런스증거 아님), 다음 독립 첫 노출3명 이상 권장. 사용자 체감도 별도 기록.
기록: 보드별 활성시간, LINE고유소거수, 공급획득/초과/소비/대기시간, 자연연쇄깊이, 파동별실피해, 선소거로 회피한표적, 파괴전후연결손실/높이, 승패/소요시간/남은HP. CHAIN단독/교차전략/LINE편중을 비교.
임시 재검토 기준: 보급0 대기 시간이 활성시간20% 초과, 대부분 참가자가 LINE을 의무 작업으로 설명, 파괴가 구축 손실보다 상시 정리 이득으로 평가, 예고 이유 오독이 반복되면 수치/빈도 재설계. 표본이 작으므로 통계적 우월성/최종재미 확정 금지.

## 9. SWOT와 강화 작업

| 항목 | 현재 근거 | 강화/보완 | 검증과 위험 |
|---|---|---|---|
| S 준비-발동의 역할 분담 | 기존4문양/예고/계열스킬 재사용 가능 | 보급쌍을 같은 LINE결과에 묶고 선발동/시간연장 선택 제공 | 자율전환 이유를 플레이어가 설명하는지; 단순 왕복숙제 위험 |
| W CHAIN독립 무한발동 | 실제 무료 보충과 무조건 swap진입 | 자동보충 폐기, 유한배치와 LINE보급 | 공급/사고시간 계측; 대기20%초과 재검토 |
| W 두 낙하퍼즐의 조작 유사성 | 둘 다 이동/회전/낙하가 됨 | LINE은 빈틈채움/즉시자원,CHAIN은 색연결/미래연쇄; 목표·배치언어 분리 | 둘의 목적을 튜토리얼 후 혼동하는지 |
| O 적 패턴이 보드 계획을 바꿈 | 기존 HP피해 중심행동에서 확장 | 고정표적 예고, 미리소거/시간연장 대응 | 대응 성공을 로그와 짧은피드백으로 표시 |
| T 무작위가 불공정하게 느껴짐 | 조립한연쇄 파괴 가능 | 2초예고·IDs고정·재추첨없음·첫전투면제 | 예측불가불만, 피해/파괴겹침의 난도급증 |
| T 삭제가 오히려 보상/무한루프 | 중력연결과 원인중복 가능 | cause전파·무보상정리·ID중복방지 | AC07~11과 이벤트fuzz; 항상순손해라고 약속하지 않음 |
| T 외형/용어/구현범위의 모방 | 장르참고와 원작표현 혼동 | 독자룬석/기존세계관, 범용규칙만재구현 | 최종자산/출시검토 별도, 보장주장금지 |

## 10. 남은 작업 판정

| 작업 | 현재 | 처리 |
|---|---|---|
| 검증 저장 격리 | 직전 로컬405검사 통과, 미커밋/검토미완료 | 코드보존. R3개발 전 안전검증/별도출판 마무리; 이전원정복구 미확인 |
| R3낙하/회전/연결 | 없음 | 신규구현 최우선 |
| LINE보급연결 | 없음 | 권장안 상세확정 후 연결 |
| 적파괴/예고/중복방지 | 없음 | R3보드 ID/트랜잭션 뒤 구현 |
| 자동스킬 | R2구현 | 효과재사용,발동원인/계열잠금 경계 변경 |
| 저장/원정/재개 | R2구현 | 보드schema변경으로 재검증/별도R3저장 |
| 전투UI/조작/연습 | R2구현 | 8×8교환 및3매치 설명 대체 |
| 감시탑6포즈 | PR116구현/화면/패키지 | 보존/재사용; 최종아트승인 별도 |
| 주조소 등 공용적그림 | 일부공용 | 개별소비처 확인 후 필요한상태군만제작 |
| 타일/파괴표식/모션 | 기존4문양 | 룬쌍/표적/피격/소거상태 제작 및연결 |
| 음향 | 효과/BGM구현 | 파괴/배치효과·청취믹스 검증, 게임시간분리유지 |
| 블루프린트 | 기존52p는과거소스 | 승인된R3명세/표/아틀라스/실제캡처를 새source-bound판으로갱신; 지금기존PDF덮어쓰기안함 |
| 폐기후보 | R2swap입력/자동리필/3매치학습 | R3전환 검증후 활성경로에서만제거. R2호환코드/기록은 유지, 삭제후보는 사용자삭제대기폴더 |

상태 요약: 방향은 확정, 상세안은 SPECIFIED_FOR_REVIEW, 기능은 NOT_IMPLEMENTED. 테스트명세가 있는 것과 테스트를 실행한 것은 다르다. 전체게임 완성·원정복구·재미·최종비주얼·출시 PASS를 주장하지 않는다.
