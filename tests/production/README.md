# Production test namespace

Tests under `tests/production/` validate the current Production canon only.

They are intentionally separate from the historical Core Combat Foundation / Engineering Harness tests under `tests/unit` and `tests/integration`.

Current production authority is routed by `docs/design/PRODUCTION_CANON_INDEX.json`.
